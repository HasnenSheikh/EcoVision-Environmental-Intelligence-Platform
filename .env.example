# Copy this file to .env and fill in values for local development.
# On Render these are set via the dashboard / render.yaml envVars.

# Flask secret key – use a long random string in production
SECRET_KEY=change-me-to-a-long-random-string

# Set to 'production' on Render, 'development' locally
FLASK_ENV=development

# Port – Render sets this automatically; leave blank for local default (5000)
# PORT=5000
